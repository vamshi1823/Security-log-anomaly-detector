name: Keep Render App Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app to keep it awake
        run: curl -sSf https://security-log-anomaly-detector.onrender.com/api/stats || true